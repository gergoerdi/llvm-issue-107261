[package]
name = "worduino-avr"
version = "0.1.0"
authors = ["Gergő ÉRDI <gergo@erdi.hu>"]

[dependencies]
avr-config = "2.0.1"
avr-std-stub = "1.0"
worduino-engine = { path = "worduino-engine" }

[profile.release]
lto = true
